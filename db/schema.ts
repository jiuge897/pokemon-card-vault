import { index, integer, real, sqliteTable, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const inventory=sqliteTable('inventory',{id:integer('id').primaryKey({autoIncrement:true}),ownerId:text('owner_id').notNull().default('e64a5304-84db-4dab-9c43-b14edc0b8a7d'),productId:integer('product_id').notNull(),name:text('name').notNull(),itemType:text('item_type').notNull().default('card'),printing:text('printing').notNull(),quantity:integer('quantity').notNull().default(1),marketPrice:real('market_price'),updatedAt:text('updated_at')},table=>[uniqueIndex('idx_inventory_owner_product_printing').on(table.ownerId,table.productId,table.printing),index('idx_inventory_owner_id').on(table.ownerId)]);
